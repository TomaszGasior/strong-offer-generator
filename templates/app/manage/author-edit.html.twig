{% extends 'app/base.html.twig' %}

{% block title %}Edytuj osobę „{{ author.name }}”{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button class="btn-primary btn">Zapisz zmiany</button>
    <a href="{{ path('author.remove', {id: author.id}) }}" class="btn btn-danger float-right">Usuń osobę</a>
    {{ form_end(form) }}
{% endblock %}
